dependencies:
  # Waveshare BSP — display, touch, SD card
  waveshare/esp32_p4_wifi6_touch_lcd_7b:
    version: "*"

  # LVGL v9
  lvgl/lvgl:
    version: "9.2.*"
    public: true

  # WiFi via ESP32-C6 coprocessor (Phase 5 — re-enable when building hotspot)
  # espressif/esp_wifi_remote:
  #   version: "0.14.*"
  # espressif/esp_hosted:
  #   version: "1.4.*"

  # Serial flasher (esptool protocol in C)
  espressif/esp-serial-flasher:
    version: "^1.11.0"

  # USB Host — CH340 serial bridge for target devices
  espressif/usb_host_cdc_acm:
    version: "^2.1.0"

  espressif/usb_host_ch34x_vcp:
    version: "^2.2.0"

  idf:
    version: ">=5.4.0"
